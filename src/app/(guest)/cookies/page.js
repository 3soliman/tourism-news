import TrustPage from '@/views/TrustPage';

export const metadata = {
  title: 'Cookie policy',
  description: 'How we use cookies and similar technologies.'
};

export default function CookiesPage() {
  return <TrustPage pageId="cookies" />;
}
